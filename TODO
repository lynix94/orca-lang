# TODO
timer
str, repr to common function
add time.sleep(1.2)

# bug
hex diff

# bug fixed
-- using namespace conflict
-- test_oo failed

# now thinking 
decode to switch
html lib
** select
root, cloud (depend zookeeper or etcd)

#channel
channel assert
channel log
channel queue
channel dist queue


# modlue
type.XXX.make(...)
  ex) type.tuple.make(1,2,3) => (1,2,3)

# storage
sqlite
memcached
leveldb

# test
channel
eval
under
anonymous
assert
log


# refactoring
length check & change code to unsigned
move parse to vm & parse, interpret lock


### done
select
fd select
fix trace.orca trace
support directory parsing (name.orca)
support main.orca.html
channel opt_assign
switch
change to lambda
add eval (interpret, parse)
improve catch statement
channel
def foo(...) under path
make anonymous def
id, members -> ID, MEMBERS (upper)



# remove
windows build
once
pure
dfsd


# add
cache





