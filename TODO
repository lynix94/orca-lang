# TODO
make repr
orm
check name of object (def a:= foo.clone())
*.TEXT
decorator
fix: hex string diff


# now thinking 
root, cloud (depend zookeeper or etcd)

#channel
channel assert
channel dist queue

# storage
cache
leveldb


### done
timer
channel log
add time.sleep(1.2)
select
fd select
fix trace.orca trace
support directory parsing (name.orca)
support main.orca.html
channel opt_assign
switch
change to lambda
add eval (interpret, parse)
improve catch statement
channel
def foo(...) under path
make anonymous def
id, members -> ID, MEMBERS (upper)
fix: using namespace conflict
fix: test_oo failed
channel queue
sqlite

# remove
windows build
once
pure
dfsd

