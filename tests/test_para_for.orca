using ipc;
using time;

list = [1..1000];
parallel for a in list.iter() {
  a( a()*2 );
}

print: list;
print: %[x*2|x<-1~1000];

if list != %[x*2|x<-1~1000] {
	throw test.para_for <- 'para for fail';
}


def tot := 0;

parallel for a in %[x|x<-1~1000] {
	ipc.mutex {
		my.tot += a();
	}
}

print: my.tot;

if my.tot != 1001 * 500 {
	throw test.para_for <- 'para for fail';
}
	

	
print: 'OK';


